;
; COMMENT OUT THE LaunchApplication call below if simply debugging the launching of the Device\AVD
;
C, LaunchApplication

; PAUSES test execution until user hits "Run" on SAFS Monitor
;C, SetVariableValues, ^SAFS_DRIVER_CONTROL="PAUSE"

C, Pause, 3

C, CloseApplication
